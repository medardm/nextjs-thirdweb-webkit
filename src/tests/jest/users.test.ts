import '@testing-library/jest-dom'
import {afterAll, beforeAll, describe, expect, it} from "@jest/globals";

beforeAll(async () => {
})

afterAll(async () => {
})

describe('Users', () => {
  it('deletes test users before starting tests', async () => {
    expect(true).toBeTruthy()
  })
})
